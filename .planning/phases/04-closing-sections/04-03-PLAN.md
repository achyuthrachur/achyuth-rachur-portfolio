---
phase: 04-closing-sections
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - src/app/page.tsx
autonomous: false
requirements: [EDU-01, EDU-02, EDU-03, EDU-04, CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, FOOT-01, FOOT-02]

must_haves:
  truths:
    - "Education section renders TiltedCard degree and two research SpotlightCards"
    - "Contact section shows GradientText heading and 4 Iconsax icon anchors"
    - "Footer renders below contact section with copyright text"
    - "npm run build exits 0 with static /out directory generated"
    - "All nav links scroll to correct sections with no layout breaks"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Complete single-page portfolio"
      contains: "EducationSection, ContactSection, Footer"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/EducationSection.tsx"
      via: "import EducationSection"
      pattern: "EducationSection"
    - from: "src/app/page.tsx"
      to: "src/components/ContactSection.tsx"
      via: "import ContactSection"
      pattern: "ContactSection"
    - from: "src/app/page.tsx"
      to: "src/components/Footer.tsx"
      via: "import Footer"
      pattern: "Footer"
---

<objective>
Wire the three new Phase 4 components into page.tsx, replacing the placeholder sections. Verify the complete build succeeds and run a human visual check.

Purpose: Complete the page end-to-end — all five sections plus footer are live.
Output: Updated src/app/page.tsx with Education, Contact, and Footer wired in.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/app/page.tsx
@.planning/phases/04-closing-sections/04-01-SUMMARY.md
@.planning/phases/04-closing-sections/04-02-SUMMARY.md
</context>

<interfaces>
<!-- What page.tsx currently has and what changes are needed. -->

Current page.tsx (placeholder sections to replace):
```tsx
<section id="education" className="scroll-mt-16 min-h-screen bg-page flex items-center justify-center">
  <p className="font-body text-tint-500 text-sm">Education — coming in Phase 4</p>
</section>

<section id="contact" className="scroll-mt-16 min-h-screen bg-[#0f172a] flex items-center justify-center">
  <p className="font-body text-tint-500 text-sm">Contact — coming in Phase 4</p>
</section>
```

Target page.tsx (after wiring):
```tsx
import { EducationSection } from '@/components/EducationSection';
import { ContactSection } from '@/components/ContactSection';
import { Footer } from '@/components/Footer';

// Education section — replace placeholder, keep id + scroll-mt-16 + bg-page
<section id="education" className="scroll-mt-16 bg-[#fafbfd]">
  <EducationSection />
</section>

// Contact section + Footer — same dark bg wrapper, Footer flows inside it
<section id="contact" className="scroll-mt-16 bg-[#0f172a]">
  <ContactSection />
  <Footer />
</section>
```

Notes:
- Remove `min-h-screen flex items-center justify-center` from both replaced sections — those were placeholder centering utilities only
- bg-[#fafbfd] matches EDU-01 (#fafbfd is surface-primary, close to bg-page)
- Footer lives inside the contact section wrapper — same dark background flows naturally
- page.tsx is a server component — no 'use client' directive
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire components into page.tsx and verify build</name>
  <files>src/app/page.tsx</files>
  <action>
Update `src/app/page.tsx` with the following changes:

1. Add three new imports after the existing imports:
```tsx
import { EducationSection } from '@/components/EducationSection';
import { ContactSection } from '@/components/ContactSection';
import { Footer } from '@/components/Footer';
```

2. Replace the education placeholder section:
```tsx
// BEFORE:
<section id="education" className="scroll-mt-16 min-h-screen bg-page flex items-center justify-center">
  <p className="font-body text-tint-500 text-sm">Education — coming in Phase 4</p>
</section>

// AFTER:
<section id="education" className="scroll-mt-16 bg-[#fafbfd]">
  <EducationSection />
</section>
```

3. Replace the contact placeholder section, including Footer inside the same dark wrapper:
```tsx
// BEFORE:
<section id="contact" className="scroll-mt-16 min-h-screen bg-[#0f172a] flex items-center justify-center">
  <p className="font-body text-tint-500 text-sm">Contact — coming in Phase 4</p>
</section>

// AFTER:
<section id="contact" className="scroll-mt-16 bg-[#0f172a]">
  <ContactSection />
  <Footer />
</section>
```

Then run the build to verify:
```bash
npm run build
```

Build must exit 0 with `/out` directory created. If TypeScript errors appear, fix them before proceeding.
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/Resume" && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>npm run build exits 0, /out directory exists, no TypeScript errors. page.tsx imports EducationSection + ContactSection + Footer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 4 complete: EducationSection (TiltedCard degree + 2 research SpotlightCards), ContactSection (GradientText heading + 4 Iconsax icon anchors), and Footer — all wired into page.tsx. Build passed.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. Scroll to the **Education** section:
   - TiltedCard shows "Purdue University" / "Bachelor of Science in Management and Data Analytics" / "December 2024"
   - Two SpotlightCards below: "The Effect of the Digital Age..." and "Securing the Future..."
   - Each has a "View Paper" link — click to confirm correct URL opens in new tab
   - Cards fade in as you scroll into view
3. Scroll to the **Contact** section:
   - Dark background (#0f172a)
   - "Let's connect" heading animates with indigo-to-white gradient
   - 4 icons visible: Personal Email, Crowe Email, LinkedIn, GitHub
   - Hover each icon — it should scale up and turn indigo (#818cf8) with Bold variant
   - Click each link — verify mailto/URL is correct
4. **Footer** appears below Contact: "© 2025 Achyuth Rachur" in muted slate text
5. Check **Nav links**: "Education" and "Contact" nav items scroll to the correct sections
  </how-to-verify>
  <resume-signal>Type "approved" to confirm visual check passes, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- npm run build exits 0
- /out directory generated (static export)
- page.tsx has no 'use client' directive (server component)
- Education section id="education" scroll-mt-16 preserved
- Contact section id="contact" scroll-mt-16 preserved
- Footer inside contact section wrapper (same dark bg)
</verification>

<success_criteria>
- All 5 sections + footer render without errors (EDU-01 through FOOT-02 all satisfied)
- Build output is clean static export for Cloudflare Pages
- Human confirms visual appearance matches spec
</success_criteria>

<output>
After completion, create `.planning/phases/04-closing-sections/04-03-SUMMARY.md`
</output>
