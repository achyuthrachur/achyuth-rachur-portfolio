---
phase: 04-closing-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ContactSection.tsx
  - src/components/Footer.tsx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, FOOT-01, FOOT-02]

must_haves:
  truths:
    - "Contact section has dark bg-[#0f172a] with animated GradientText heading"
    - "Four Iconsax icons (personal email, work email, LinkedIn, GitHub) render in a flex row"
    - "Each icon is wrapped in an anchor pointing to the correct URL/mailto"
    - "Each icon animates to #818cf8 color and scales on hover via useState + Framer Motion"
    - "Footer renders copyright text on same dark background, separated by a subtle border"
    - "Both components compile without TypeScript errors"
  artifacts:
    - path: "src/components/ContactSection.tsx"
      provides: "Contact section with icons"
      exports: ["ContactSection"]
    - path: "src/components/Footer.tsx"
      provides: "Footer copyright bar"
      exports: ["Footer"]
  key_links:
    - from: "src/components/ContactSection.tsx"
      to: "src/components/reactbits/GradientText.tsx"
      via: "import GradientText, colors prop override"
      pattern: "GradientText"
    - from: "src/components/ContactSection.tsx"
      to: "iconsax-react"
      via: "Sms, DirectboxSend, Link21, Code imports"
      pattern: "iconsax-react"
---

<objective>
Build the ContactSection and Footer components. ContactSection shows a GradientText heading and four Iconsax icons as hover-animated anchor links on a dark background. Footer shows the copyright line.

Purpose: Complete the bottom of the page — gives visitors actionable ways to reach out.
Output: src/components/ContactSection.tsx + src/components/Footer.tsx (named exports)
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

@src/components/reactbits/GradientText.tsx
@src/components/AboutSection.tsx
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/components/reactbits/GradientText.tsx:
```typescript
interface GradientTextProps {
  children: ReactNode;
  className?: string;
  colors?: string[];       // Override: ['#818cf8', '#f8fafc', '#818cf8']
  animationSpeed?: number;
  showBorder?: boolean;
}
export function GradientText(props: GradientTextProps): JSX.Element
```

From iconsax-react (installed with --legacy-peer-deps):
```typescript
import { Sms, DirectboxSend, Link21, Code } from 'iconsax-react';
// Props: color (string), variant ("Linear" | "Bold" | ...), size (number)
// Hover pattern: useState<string> for color, inline color prop (not Tailwind CSS)
```

From src/components/AboutSection.tsx (whileInView pattern):
```typescript
import { motion } from 'motion/react';
// motion.div whileHover for scale: whileHover={{ scale: 1.1 }}
```

Established section container pattern:
```tsx
<div className="max-w-5xl mx-auto px-6 py-24">
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build ContactSection component</name>
  <files>src/components/ContactSection.tsx</files>
  <action>
Create `src/components/ContactSection.tsx` as a named export client component.

Full structure:

```tsx
'use client';

import { useState } from 'react';
import { motion } from 'motion/react';
import { GradientText } from '@/components/reactbits/GradientText';
import { Sms, DirectboxSend, Link21, Code } from 'iconsax-react';

const DEFAULT_COLOR = '#f8fafc';
const HOVER_COLOR = '#818cf8';

interface ContactItem {
  label: string;
  href: string;
  Icon: React.ComponentType<{ color: string; variant: 'Linear' | 'Bold'; size: number }>;
  external: boolean;
}

const CONTACTS: ContactItem[] = [
  {
    label: 'Personal Email',
    href: 'mailto:achyuth.v.rachur@gmail.com',
    Icon: Sms,
    external: false,
  },
  {
    label: 'Crowe Email',
    href: 'mailto:achyuth.rachur@crowe.com',
    Icon: DirectboxSend,
    external: false,
  },
  {
    label: 'LinkedIn',
    href: 'https://www.linkedin.com/in/achyuth-rachur/',
    Icon: Link21,
    external: true,
  },
  {
    label: 'GitHub',
    href: 'https://github.com/achyuthrachur',
    Icon: Code,
    external: true,
  },
];

function ContactIcon({ item }: { item: ContactItem }) {
  const [hovered, setHovered] = useState(false);
  const color = hovered ? HOVER_COLOR : DEFAULT_COLOR;

  return (
    <motion.a
      href={item.href}
      {...(item.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
      className="flex flex-col items-center gap-2 group"
      onHoverStart={() => setHovered(true)}
      onHoverEnd={() => setHovered(false)}
      whileHover={{ scale: 1.1 }}
      transition={{ duration: 0.15, ease: [0.16, 1, 0.3, 1] }}
    >
      <item.Icon
        color={color}
        variant={hovered ? 'Bold' : 'Linear'}
        size={28}
      />
      <span className="text-xs font-body text-slate-400 group-hover:text-[#818cf8] transition-colors duration-150">
        {item.label}
      </span>
    </motion.a>
  );
}

export function ContactSection() {
  return (
    <div className="max-w-5xl mx-auto px-6 py-24 text-center">
      <motion.div
        initial={{ opacity: 0, y: 30 }}
        whileInView={{ opacity: 1, y: 0 }}
        viewport={{ once: true, margin: '-100px' }}
        transition={{ duration: 0.6, ease: [0.16, 1, 0.3, 1] }}
      >
        <h2 className="text-3xl font-semibold font-body">
          <GradientText colors={['#818cf8', '#f8fafc', '#818cf8']} animationSpeed={6}>
            Let&apos;s connect
          </GradientText>
        </h2>
        <p className="text-sm font-body text-slate-400 mt-3 mb-10">
          Open to new projects, conversations, and opportunities.
        </p>
        <div className="flex flex-row gap-10 justify-center flex-wrap">
          {CONTACTS.map((item, i) => (
            <motion.div
              key={item.label}
              initial={{ opacity: 0, y: 20 }}
              whileInView={{ opacity: 1, y: 0 }}
              viewport={{ once: true }}
              transition={{ delay: i * 0.08, duration: 0.5, ease: [0.16, 1, 0.3, 1] }}
            >
              <ContactIcon item={item} />
            </motion.div>
          ))}
        </div>
      </motion.div>
    </div>
  );
}
```

Key rules:
- `'use client'` at top — uses useState + motion
- Named export only: `export function ContactSection`
- Helper `ContactIcon` component NOT exported (local only)
- Icon color managed via useState, NOT Tailwind classes — Iconsax ignores Tailwind for color
- `variant` switches from `'Linear'` to `'Bold'` on hover (satisfies CONT-05)
- `motion.a` handles both hover scale AND renders the anchor tag (CONT-04)
- No phone number (explicitly excluded per CONTEXT.md)
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/Resume" && npx tsc --noEmit 2>&1 | grep -i "ContactSection\|error" | head -20</automated>
  </verify>
  <done>src/components/ContactSection.tsx exists, TypeScript clean, exports ContactSection named function with 4 contact icons</done>
</task>

<task type="auto">
  <name>Task 2: Build Footer component</name>
  <files>src/components/Footer.tsx</files>
  <action>
Create `src/components/Footer.tsx` as a named export. Footer flows directly under the Contact section inside the same dark `bg-[#0f172a]` wrapper in page.tsx, so no background color here — just the divider and text.

```tsx
export function Footer() {
  return (
    <footer className="border-t border-[rgba(248,250,252,0.08)] py-6 text-center">
      <p className="text-sm font-body text-slate-500">
        &copy; 2025 Achyuth Rachur
      </p>
    </footer>
  );
}
```

Rules:
- No `'use client'` needed — pure static markup
- Named export only: `export function Footer`
- `border-t border-[rgba(248,250,252,0.08)]` matches the subtle divider specified in CONTEXT.md
- `py-6` for padding (FOOT-01)
- `text-sm text-slate-500` for muted copyright (FOOT-02)
- `text-center` centers the content
- Use HTML entity `&copy;` for the copyright symbol (not ©) to avoid encoding issues
  </action>
  <verify>
    <automated>cd "C:/Users/RachurA/OneDrive - Crowe LLP/VS Code Programming Projects/Resume" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>src/components/Footer.tsx exists, exports Footer named function, npm run build exits 0 with no errors</done>
</task>

</tasks>

<verification>
- npm run build passes with no TypeScript errors
- ContactSection has GradientText heading with colors=['#818cf8', '#f8fafc', '#818cf8']
- All 4 contact links present: 2x mailto, LinkedIn, GitHub (no phone)
- Iconsax icons use inline color prop via useState — not Tailwind CSS classes
- Footer has border-t separator and text-slate-500 copyright text
- All motion imports from motion/react (not framer-motion)
</verification>

<success_criteria>
- ContactSection: dark background (applied by page.tsx wrapper), GradientText heading, 4 Iconsax icon anchors (CONT-01, CONT-02, CONT-03, CONT-04)
- Hover: scale 1.1 + color #818cf8 + variant Bold (CONT-05)
- Footer: py-6, muted copyright text on Indigo Dark bg (FOOT-01, FOOT-02)
</success_criteria>

<output>
After completion, create `.planning/phases/04-closing-sections/04-02-SUMMARY.md`
</output>
