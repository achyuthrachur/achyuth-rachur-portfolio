---
phase: 01-foundation
plan: 01
subsystem: infra
tags: [nextjs, typescript, tailwind, eslint, static-export, cloudflare-pages]

# Dependency graph
requires: []
provides:
  - Next.js 16.1.6 project scaffolded in Resume/ directory with App Router, TypeScript, Tailwind v4
  - Static export configuration (output: export) for Cloudflare Pages deployment
  - Root layout with Achyuth Rachur metadata and font-body body class
  - Minimal branded placeholder page (bg-page, font-body, text-tint-900)
  - Build verified: /out/index.html present after npm run build
affects: [02-design-tokens, 03-components, 04-content, 05-deploy]

# Tech tracking
tech-stack:
  added: [next@16.1.6, react@19.2.3, react-dom@19.2.3, typescript@5, tailwindcss@4, eslint@9, eslint-config-next@16.1.6, "@tailwindcss/postcss@4"]
  patterns:
    - App Router (src/app/) with TypeScript and import alias @/*
    - Static export via output: 'export' in next.config.ts
    - Tailwind v4 with @import "tailwindcss" (no tailwind.config.ts needed in v4)
    - Turbopack used by Next.js 16 for production builds

key-files:
  created:
    - src/app/layout.tsx
    - src/app/page.tsx
    - src/app/globals.css
    - next.config.ts
    - tsconfig.json
    - package.json
    - .gitignore
  modified: []

key-decisions:
  - "Scaffolded to temp lowercase directory (resume-temp) then moved files — create-next-app cannot use directories with capital letters (npm naming restriction)"
  - "Package named achyuth-rachur-portfolio (lowercase per npm requirements) not Resume"
  - "next-env.d.ts excluded from git — it is in .gitignore (auto-generated by Next.js)"
  - "Tailwind v4 uses @import 'tailwindcss' in CSS instead of tailwind.config.ts — no config file generated"
  - "Turbopack is default build system in Next.js 16 (replaced webpack)"
  - "globals.css left as generated — will be fully replaced in Plan 02"

patterns-established:
  - "Static export pattern: output: 'export' + images.unoptimized: true for Cloudflare Pages"
  - "Crowe brand class placeholders: bg-page, font-body, text-tint-900 used in page.tsx (defined in Plan 02)"

requirements-completed: [SCAF-01, SCAF-02]

# Metrics
duration: 19min
completed: 2026-03-04
---

# Phase 1 Plan 01: Next.js 16 Project Scaffold Summary

**Next.js 16.1.6 with App Router, TypeScript, Tailwind v4, static export configured for Cloudflare Pages — build produces /out/index.html with 0 errors**

## Performance

- **Duration:** 19 min
- **Started:** 2026-03-04T01:42:10Z
- **Completed:** 2026-03-04T02:01:16Z
- **Tasks:** 2
- **Files modified:** 15 created, 1 modified (package.json name field)

## Accomplishments
- Next.js 16.1.6 project scaffolded with App Router, TypeScript, Tailwind v4, ESLint, src/ directory and @/* import alias
- Replaced default layout.tsx with Achyuth Rachur portfolio metadata and font-body body class
- Replaced default page.tsx with minimal Crowe-branded placeholder using Tailwind brand tokens
- Configured next.config.ts with output: 'export' and images.unoptimized: true for Cloudflare Pages
- Build verified: npm run build exits 0, /out/index.html present

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold Next.js project** - `446be6b` (feat)
2. **Task 2: Configure static export** - `f9d0743` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src/app/layout.tsx` - Root layout with Achyuth Rachur metadata and font-body antialiased body class
- `src/app/page.tsx` - Minimal branded placeholder (bg-page, font-body, text-tint-900)
- `src/app/globals.css` - Generated by create-next-app with @import "tailwindcss" (to be replaced in Plan 02)
- `next.config.ts` - Static export config with output: 'export' and images.unoptimized: true
- `tsconfig.json` - TypeScript config with strict mode and @/* import alias
- `package.json` - Project dependencies (name: achyuth-rachur-portfolio)
- `package-lock.json` - Dependency lockfile
- `.gitignore` - Standard Next.js ignore patterns
- `eslint.config.mjs` - ESLint configuration
- `postcss.config.mjs` - PostCSS config for Tailwind v4
- `public/` - Default Next.js public assets (SVGs)

## Decisions Made
- **Workaround for npm naming restriction:** `create-next-app` uses the directory name as the npm package name. "Resume" contains a capital letter, which npm rejects. Scaffolded to a temp lowercase directory `resume-temp`, then manually moved files to `Resume/`. Package name set to `achyuth-rachur-portfolio`.
- **No tailwind.config.ts generated:** Tailwind v4 does not require a config file. It uses `@import "tailwindcss"` in CSS. Custom brand tokens (colors, fonts, shadows) will be added via `@theme` directive in Plan 02 globals.css.
- **Turbopack note:** Next.js 16 defaults to Turbopack for both dev and production builds. A workspace root warning was shown (multiple lockfiles detected), but this did not affect the build.
- **globals.css left as generated:** Will be fully replaced in Plan 02 when Crowe brand tokens are added.
- **No src/fonts/ directory:** No Helvetica Now font files available; Arial fallback stack will be configured in Plan 02.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Scaffolded to temp directory due to npm naming restriction**
- **Found during:** Task 1 (scaffold Next.js project)
- **Issue:** `create-next-app` uses the parent directory name as npm package name. "Resume" has a capital letter, which npm no longer allows (`name can no longer contain capital letters`)
- **Fix:** Scaffolded to `../resume-temp`, copied all generated files to `Resume/`, set package name to `achyuth-rachur-portfolio` in package.json, then removed temp directory
- **Files modified:** package.json (name field only)
- **Verification:** `npm install` succeeded, `npm run build` succeeded
- **Committed in:** 446be6b (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (blocking)
**Impact on plan:** The workaround was transparent — all scaffold files landed in the correct locations. No architectural change. Output is identical to what `create-next-app .` would have produced.

## Issues Encountered
- npm naming restriction on capital letters in directory names required temp directory workaround (documented above as deviation)
- Turbopack workspace root warning during build — harmless, caused by a lockfile at C:\Users\RachurA\package-lock.json (unrelated user-level file). Does not affect build output.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Next.js project is running and builds to static HTML
- Plan 02 can proceed immediately to add Crowe brand tokens to globals.css
- No blockers

---
*Phase: 01-foundation*
*Completed: 2026-03-04*

## Self-Check: PASSED

All files and commits verified:
- src/app/layout.tsx: FOUND
- src/app/page.tsx: FOUND
- src/app/globals.css: FOUND
- next.config.ts: FOUND
- tsconfig.json: FOUND
- package.json: FOUND
- .gitignore: FOUND
- out/: FOUND
- out/index.html: FOUND
- Commit 446be6b (Task 1): FOUND
- Commit f9d0743 (Task 2): FOUND
