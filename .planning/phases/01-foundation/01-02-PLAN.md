---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/globals.css
  - src/components/ui/badge.tsx
autonomous: true
requirements:
  - SCAF-03
  - SCAF-04

must_haves:
  truths:
    - "shadcn/ui is initialized and the Badge component is importable from '@/components/ui/badge'"
    - "globals.css contains the Crowe HSL shadcn overrides inside @layer base (--primary: 215 98% 13%, etc.)"
    - "globals.css contains a @theme block with all Crowe color tokens, tint scale, background aliases, font families, and shadow system"
    - "Tailwind utility classes bg-crowe-indigo-dark, text-crowe-amber, shadow-crowe-card, font-display, bg-page all generate valid CSS"
    - "The prefers-reduced-motion media query is present in globals.css"
    - "html has scroll-behavior: smooth"
    - "npm run build exits 0 after globals.css changes"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Complete Crowe brand token system — shadcn HSL vars + raw CSS vars + Tailwind @theme block + accessibility"
      contains: "@theme"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge component themed to Crowe palette"
      exports: ["Badge", "badgeVariants"]
  key_links:
    - from: "src/app/globals.css @layer base :root"
      to: "src/components/ui/badge.tsx"
      via: "CSS variables --primary, --secondary, --border read by shadcn Badge"
      pattern: "--primary: 215"
    - from: "src/app/globals.css @theme"
      to: "src/app/page.tsx"
      via: "Generates bg-page, font-body, text-tint-900 Tailwind utility classes"
      pattern: "--color-page:"
---

<objective>
Initialize shadcn/ui and apply full Crowe brand token system to globals.css. This plan writes the complete design token layer: (1) shadcn HSL overrides for component theming, (2) raw Crowe CSS custom properties, (3) Tailwind v4 @theme block generating all Crowe utility classes, and (4) accessibility/scroll base overrides.

Purpose: After this plan, every Tailwind class from DESIGN.md works (bg-crowe-indigo-dark, shadow-crowe-card, etc.) and shadcn components render in Crowe colors automatically.
Output: Complete globals.css and Badge component installed at src/components/ui/badge.tsx.
</objective>

<execution_context>
@C:/Users/RachurA/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RachurA/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui and write complete globals.css</name>
  <files>src/app/globals.css, components.json</files>
  <action>
Step 1 — Run shadcn init:

```bash
npx shadcn@latest init
```

When prompted:
- Style: Default (New York if asked — either works)
- Base color: Neutral (we will override with Crowe HSL vars immediately after)
- CSS variables: Yes

If shadcn init fails with ERESOLVE (React 19 peer dep conflict):
```bash
npx shadcn@latest init --legacy-peer-deps
```

Step 2 — REPLACE `src/app/globals.css` entirely with the following content. This is the complete, final globals.css for Phase 1. Do NOT abbreviate or truncate any token — copy verbatim:

```css
/* src/app/globals.css */

/* ══════════════════════════════════════════════════════════════════
   1. TAILWIND v4 IMPORT
   Replaces the old @tailwind base; components; utilities directives
   ══════════════════════════════════════════════════════════════════ */
@import "tailwindcss";

/* ══════════════════════════════════════════════════════════════════
   2. shadcn/ui HSL OVERRIDES — Crowe Brand Theme
   These CSS variables are read by shadcn components automatically.
   Values map to Crowe Indigo Dark as primary, Amber as secondary.
   ══════════════════════════════════════════════════════════════════ */
@layer base {
  :root {
    --background: 225 33% 98%;
    --foreground: 228 20% 22%;
    --card: 225 50% 99%;
    --card-foreground: 228 20% 22%;
    --popover: 0 0% 100%;
    --popover-foreground: 228 20% 22%;
    --primary: 215 98% 13%;
    --primary-foreground: 225 33% 97%;
    --secondary: 39 100% 48%;
    --secondary-foreground: 215 98% 13%;
    --muted: 225 20% 96%;
    --muted-foreground: 228 10% 37%;
    --accent: 39 100% 48%;
    --accent-foreground: 215 98% 13%;
    --destructive: 341 79% 56%;
    --destructive-foreground: 225 33% 97%;
    --border: 226 17% 89%;
    --input: 226 17% 89%;
    --ring: 215 100% 19%;
    --radius: 0.75rem;
  }
}

/* ══════════════════════════════════════════════════════════════════
   3. CROWE RAW CSS CUSTOM PROPERTIES
   Direct :root variables for use in any CSS value.
   These are in addition to the @theme Tailwind tokens below.
   Source: DESIGN.md Section 1.2
   ══════════════════════════════════════════════════════════════════ */
:root {
  /* Primary Colors */
  --crowe-amber-bright: #FFD231;
  --crowe-amber-core: #F5A800;
  --crowe-amber-dark: #D7761D;
  --crowe-indigo-bright: #003F9F;
  --crowe-indigo-core: #002E62;
  --crowe-indigo-dark: #011E41;

  /* Secondary Colors */
  --crowe-teal-bright: #16D9BC;
  --crowe-teal-core: #05AB8C;
  --crowe-teal-dark: #0C7876;
  --crowe-cyan-light: #8FE1FF;
  --crowe-cyan-core: #54C0E8;
  --crowe-cyan-dark: #007DA3;
  --crowe-blue-light: #32A8FD;
  --crowe-blue-core: #0075C9;
  --crowe-blue-dark: #0050AD;
  --crowe-violet-bright: #EA80FF;
  --crowe-violet-core: #B14FC5;
  --crowe-violet-dark: #612080;
  --crowe-coral-bright: #FF526F;
  --crowe-coral-core: #E5376B;
  --crowe-coral-dark: #992A5C;

  /* Warm Neutral Tints */
  --crowe-white: #FFFFFF;
  --crowe-black: #000000;
  --crowe-tint-950: #1a1d2b;
  --crowe-tint-900: #2d3142;
  --crowe-tint-700: #545968;
  --crowe-tint-500: #8b90a0;
  --crowe-tint-300: #c8cbd6;
  --crowe-tint-200: #dfe1e8;
  --crowe-tint-100: #eef0f4;
  --crowe-tint-50: #f6f7fa;

  /* Semantic Tokens */
  --color-text-primary: var(--crowe-tint-900);
  --color-text-secondary: var(--crowe-tint-700);
  --color-text-muted: var(--crowe-tint-500);
  --color-text-inverse: #f6f7fa;
  --color-text-brand: var(--crowe-indigo-dark);

  /* Surface Tokens */
  --color-surface-page: #f8f9fc;
  --color-surface-primary: #fafbfd;
  --color-surface-secondary: var(--crowe-tint-50);
  --color-surface-elevated: var(--crowe-white);
  --color-surface-brand: var(--crowe-indigo-dark);
  --color-surface-brand-soft: #f0f2f8;
  --color-surface-accent: var(--crowe-amber-core);
  --color-surface-amber-soft: #fff8eb;

  --color-accent-primary: var(--crowe-amber-core);
  --color-accent-primary-hover: var(--crowe-amber-dark);
  --color-accent-secondary: var(--crowe-indigo-core);

  /* Border Tokens */
  --color-border: var(--crowe-tint-200);
  --color-border-strong: var(--crowe-tint-300);

  /* Functional */
  --color-success: #05AB8C;
  --color-warning: var(--crowe-amber-core);
  --color-error: var(--crowe-coral-core);
  --color-info: var(--crowe-blue-core);

  /* Duration Tokens */
  --duration-instant: 75ms;
  --duration-fast: 150ms;
  --duration-normal: 250ms;
  --duration-slow: 350ms;
  --duration-slower: 500ms;

  /* Easing Tokens */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in: cubic-bezier(0.7, 0, 0.84, 0);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);

  /* Border Radius Tokens */
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-2xl: 20px;
  --radius-full: 9999px;
}

/* ══════════════════════════════════════════════════════════════════
   4. TAILWIND v4 @theme BLOCK
   Translates DESIGN.md tailwind.config.ts into Tailwind v4 CSS-first format.
   --color-* → bg-*, text-*, border-* utilities
   --font-* → font-* utilities
   --shadow-* → shadow-* utilities
   NOTE: Font fallback uses Arial chain (no Helvetica Now license available).
   ══════════════════════════════════════════════════════════════════ */
@theme {
  /* ── Crowe Primary Color Palette ── */
  --color-crowe-amber-bright: #FFD231;
  --color-crowe-amber: #F5A800;
  --color-crowe-amber-dark: #D7761D;
  --color-crowe-indigo-bright: #003F9F;
  --color-crowe-indigo: #002E62;
  --color-crowe-indigo-dark: #011E41;

  /* ── Crowe Secondary Color Palette ── */
  --color-crowe-teal-bright: #16D9BC;
  --color-crowe-teal: #05AB8C;
  --color-crowe-teal-dark: #0C7876;
  --color-crowe-cyan-light: #8FE1FF;
  --color-crowe-cyan: #54C0E8;
  --color-crowe-cyan-dark: #007DA3;
  --color-crowe-blue-light: #32A8FD;
  --color-crowe-blue: #0075C9;
  --color-crowe-blue-dark: #0050AD;
  --color-crowe-violet-bright: #EA80FF;
  --color-crowe-violet: #B14FC5;
  --color-crowe-violet-dark: #612080;
  --color-crowe-coral-bright: #FF526F;
  --color-crowe-coral: #E5376B;
  --color-crowe-coral-dark: #992A5C;

  /* ── Warm Tint Scale ── */
  --color-tint-950: #1a1d2b;
  --color-tint-900: #2d3142;
  --color-tint-700: #545968;
  --color-tint-500: #8b90a0;
  --color-tint-300: #c8cbd6;
  --color-tint-200: #dfe1e8;
  --color-tint-100: #eef0f4;
  --color-tint-50: #f6f7fa;

  /* ── Background Color Aliases ── */
  --color-page: #f8f9fc;
  --color-section: #f6f7fa;
  --color-section-warm: #f0f2f8;
  --color-section-amber: #fff8eb;

  /* ── Font Families ── */
  /* Using Arial fallback chain — no Helvetica Now license available */
  --font-display: Arial, 'Helvetica Neue', Helvetica, system-ui, sans-serif;
  --font-body: Arial, 'Helvetica Neue', Helvetica, system-ui, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', 'IBM Plex Mono', Consolas, monospace;

  /* ── Shadow System (indigo-tinted, not pure black) ── */
  --shadow-crowe-sm: 0 1px 3px rgba(1, 30, 65, 0.06), 0 1px 2px rgba(1, 30, 65, 0.04);
  --shadow-crowe-md: 0 4px 8px -2px rgba(1, 30, 65, 0.06), 0 2px 4px -1px rgba(1, 30, 65, 0.04);
  --shadow-crowe-lg: 0 6px 16px -4px rgba(1, 30, 65, 0.07), 0 4px 6px -2px rgba(1, 30, 65, 0.04);
  --shadow-crowe-xl: 0 12px 32px -8px rgba(1, 30, 65, 0.08), 0 8px 16px -4px rgba(1, 30, 65, 0.03);
  --shadow-crowe-hover: 0 8px 24px -4px rgba(1, 30, 65, 0.10), 0 4px 8px -2px rgba(1, 30, 65, 0.04);
  --shadow-crowe-card: 0 1px 3px rgba(1, 30, 65, 0.04), 0 6px 16px rgba(1, 30, 65, 0.04), 0 12px 32px rgba(1, 30, 65, 0.02);
  --shadow-amber-glow: 0 4px 16px rgba(245, 168, 0, 0.20);
}

/* ══════════════════════════════════════════════════════════════════
   5. BASE OVERRIDES
   ══════════════════════════════════════════════════════════════════ */
html {
  scroll-behavior: smooth;
}

body {
  background-color: #f8f9fc;
  color: #2d3142;
}

/* ══════════════════════════════════════════════════════════════════
   6. ACCESSIBILITY — Reduced Motion
   Disables all animations when user prefers reduced motion.
   Applies to Anime.js, motion/react (framer-motion), and CSS transitions.
   ══════════════════════════════════════════════════════════════════ */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

IMPORTANT: If shadcn init already generated a globals.css with `@layer base` containing shadcn vars, the replacement above includes all of that content plus the Crowe tokens. The replacement is complete — do not merge, just replace entirely.

Also check: If shadcn init generated a `tailwind.config.ts` or `tailwind.config.js` for the Crowe theme overrides, leave it (it may be needed for plugin registration) but confirm that the `@theme` block in globals.css is the source of truth for all Crowe color/shadow/font tokens. Tailwind v4 reads `@theme` from CSS — the config file is secondary.
  </action>
  <verify>
    <automated>grep -q "@theme" src/app/globals.css && grep -q "crowe-indigo" src/app/globals.css && grep -q "prefers-reduced-motion" src/app/globals.css && echo "globals.css OK"</automated>
  </verify>
  <done>globals.css contains: `@import "tailwindcss"`, `@layer base` with shadcn HSL vars (--primary: 215 98% 13%), `@theme` block with all Crowe color/shadow/font tokens, and `prefers-reduced-motion` media query.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn Badge component</name>
  <files>src/components/ui/badge.tsx</files>
  <action>
Install only the Badge component from shadcn (the only shadcn component needed in Phase 1, per CONTEXT.md decisions):

```bash
npx shadcn@latest add badge
```

If this fails with ERESOLVE (React 19 peer dep conflict):
```bash
npx shadcn@latest add badge --legacy-peer-deps
```

After install, verify `src/components/ui/badge.tsx` exists and exports `Badge` and `badgeVariants`.

Then run the build to confirm globals.css changes + Badge component compile cleanly together:

```bash
npm run build
```

Expected: Build succeeds, `/out` is produced. If any TypeScript errors appear in `src/components/ui/badge.tsx`, they are likely due to the HSL token format change — check that the shadcn component reads from CSS variables (it should, via `cn()` and Tailwind classes like `bg-primary`).

Do NOT install any other shadcn components in this phase (Dialog, Card, etc. are installed on-demand in Phases 3-4).
  </action>
  <verify>
    <automated>test -f src/components/ui/badge.tsx && npm run build && test -d out && echo "Badge installed and build OK"</automated>
  </verify>
  <done>`src/components/ui/badge.tsx` exists with `Badge` and `badgeVariants` exports. `npm run build` exits 0. `/out` directory produced without errors.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -q "@theme" src/app/globals.css && echo "@theme block present"`
2. `grep -q "crowe-indigo-dark.*#011E41" src/app/globals.css && echo "Crowe tokens present"`
3. `grep -q "prefers-reduced-motion" src/app/globals.css && echo "reduced motion present"`
4. `grep -q "scroll-behavior: smooth" src/app/globals.css && echo "smooth scroll present"`
5. `test -f src/components/ui/badge.tsx && echo "Badge component present"`
6. `npm run build && test -d out && echo "build succeeds"`
</verification>

<success_criteria>
- globals.css has all 4 sections in correct order: @import tailwindcss, @layer base (shadcn HSL), :root (raw Crowe vars), @theme (Tailwind v4 tokens), accessibility
- @theme block contains all: crowe primary/secondary colors, tint scale, background aliases, font families, all shadow variants
- shadcn Badge is at src/components/ui/badge.tsx and importable via `import { Badge } from '@/components/ui/badge'`
- `npm run build` exits 0 — no TypeScript or Tailwind compilation errors after globals.css replacement
- `grep "crowe-indigo-dark" src/app/globals.css` returns matches in both the :root and @theme sections
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- What shadcn init generated (auto-detected Tailwind v4 or not, whether it wrote @theme itself)
- The exact structure of globals.css produced (confirm all 4 sections present)
- Whether any tailwind.config.ts was generated by shadcn and what it contains
- Whether Badge install needed --legacy-peer-deps
- Confirmation that `npm run build` passes
</output>
